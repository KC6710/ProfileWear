{{ header }}
{{ column_left }}
<div id="content">
    <div class="page-header">
        <div class="container-fluid">
            <div class="float-end">
                <button type="submit" class="btn btn-primary" form="form" data-bs-toggle="tooltip" title="{{ button_save }}"><i class="fa fa-save"></i></button>
                <a href="{{ mapping }}" class="btn btn-warning" data-bs-toggle="tooltip" title="{{ button_mapping }}"><i class="fa fa-tags"></i></a>
                <a href="{{ shipping_taxes }}" class="btn btn-warning" data-bs-toggle="tooltip" title="{{ button_shipping_taxes }}"><i class="fa fa-truck"></i></a>
                <a href="{{ campaign }}" class="btn btn-warning" data-bs-toggle="tooltip" title="{{ button_campaign }}"><i class="fa fa-cogs"></i></a>
                <a href="{{ text_video_tutorial_url_setup }}" target="_blank" class="btn btn-info" data-bs-toggle="tooltip" title="{{ button_video_tutorial_setup }}"><i class="fa fa-video-camera"></i></a>
                <a href="{{ cancel }}" class="btn btn-light" data-bs-toggle="tooltip" title="{{ button_cancel }}"><i class="fa fa-reply"></i></a>
            </div> 
            <h1>{{ heading_title }}</h1>
            <ul class="breadcrumb">
                {% for breadcrumb in breadcrumbs %}
                <li class="breadcrumb-item"><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
                {% endfor %}
            </ul>
        </div>
    </div>
    <div class="container-fluid">
        {% if success %}
            <div class="alert alert-success alert-dismissible" role="alert"><i class="fa fa-check-circle"></i>&nbsp;{{ success }}
            <button type="button" class="btn-close float-end" data-bs-dismiss="alert" aria-label="{{ text_close }}"></button>
            </div>
        {% endif %}
        <div id="blockerError" style="display: none;" class="alert alert-danger">
            <i class="fa fa-exclamation-triangle"></i>&nbsp;{{ error_adblock }}
        </div>
        {% if error %}
            <div class="alert alert-danger alert-dismissible" role="alert"><i class="fa fa-exclamation-triangle"></i>&nbsp;{{ error }}
                <button type="button" class="btn-close float-end" data-bs-dismiss="alert" aria-label="{{ text_close }}"></button>
            </div>
        {% endif %}
        <div id="warning-container">
            {% if warning %}
                <div class="alert alert-warning alert-dismissible" role="alert"><i class="fa fa-info-circle"></i>&nbsp;{{ warning }}
                    <button type="button" class="btn-close float-end" data-bs-dismiss="alert" aria-label="{{ text_close }}"></button>
                </div>
            {% endif %}
        </div>
        <div id="alert-container"></div>
        <div class="card">
            <div class="card-header">
                <i class="fa fa-pencil"></i>&nbsp;<span>{{ text_panel_heading }}</span>
            </div>
            <div class="card-body">
                <form action="{{ action }}" method="post" enctype="multipart/form-data" id="form">
                    <div class="tabbable">
                        <ul class="nav nav-tabs mainMenuTabs">
                            <li class="nav-item"><a href="#tab_ads" class="nav-link active" data-bs-toggle="tab">{{ tab_text_ads }}</a></li>
                            <li class="nav-item"><a href="#tab_reports" class="nav-link" data-bs-toggle="tab">{{ tab_text_reports }}</a></li>
                            <li class="nav-item"><a href="#tab_settings" class="nav-link" data-bs-toggle="tab">{{ tab_text_settings }}</a></li>
                        </ul>
                        <div class="tab-content">
                            <div class="tab-pane" id="tab_settings">{{ tab_settings }}</div>
                            <div class="tab-pane active" id="tab_ads">{{ tab_ads }}</div>
                            <div class="tab-pane" id="tab_reports">{{ tab_reports }}</div>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
<style type="text/css">
    .adBanner {
        background-color: transparent;
        height: 1px;
        width: 1px;
    }
</style>
<div id="wrapTest">
    <div class="adBanner">
    </div>
</div>
<script type="text/javascript">
(function($) {
    $(document).ready(function() {
        $('#blockerError').toggle($("#wrapTest").height() == 0);
    });
})(jQuery);
</script>
{{ footer }}